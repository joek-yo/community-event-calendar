<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <h1>Community Event Calendar</h1>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/events">Events</a></li>
                <li><a href="/submit">Submit Event</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <div class="jumbotron text-center">
            <h2>Find and share local events happening in your community.</h2>
        </div>
        <div class="events">
            <% events.forEach(event => { %>
                <div class="event">
                    <h3><%= event.name %></h3>
                    <p><%= event.date %> at <%= event.time %></p>
                    <p><%= event.location %></p>
                    <p><%= event.description %></p>
                    <a href="/events/<%= event.id %>">View Details</a>
                </div>
            <% }) %>
        </div>
    </main>
    <footer>
        <p>&copy; 2024 Community Event Calendar. All rights reserved.</p>
    </footer>
</body>
</html>
